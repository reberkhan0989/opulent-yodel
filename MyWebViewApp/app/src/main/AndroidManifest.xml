<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mywebviewapp">
    <!-- Clearly described permissions -->
    <uses-permission 
        android:name="android.permission.INTERNET"
        android:description="@string/permission_internet_description" />
    <uses-permission 
        android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
        android:description="@string/permission_notification_description" />
    <uses-permission 
        android:name="android.permission.RECEIVE_BOOT_COMPLETED"
        android:description="@string/permission_boot_description" />
        
    <!-- App can process SMS but should not be restricted to devices with SMS hardware.
         Marking this feature optional prevents Play Store/device compatibility filtering
         for devices without telephony (tablets, some Chromebooks). -->
    <uses-feature
        android:name="android.software.sms"
        android:required="false" />

    <application
        android:name=".MyWebViewApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyWebViewApp">

        <!-- Splash Activity (Launch Activity) -->
        <activity 
            android:name=".SplashActivity" 
            android:exported="true"
            android:theme="@style/Theme.MyWebViewApp.Splash"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activities -->
        <activity android:name=".IntroActivity" android:exported="true" />
        <activity android:name=".MainActivity" />
        
        <!-- Privacy Related Activities -->
        <activity 
            android:name=".PrivacyPolicyActivity" 
            android:label="Privacy Policy"
            android:parentActivityName=".MainActivity" />
        <activity 
            android:name=".PrivacyConsentActivity"
            android:exported="true" />
        <activity
            android:name=".NotificationPermissionGuideActivity"
            android:label="Enable Notification Access"
            android:exported="true" />

        <!-- Debug Activities -->
        <activity 
            android:name=".CrashViewerActivity" 
            android:exported="false" />

        <!-- Services -->
        <service 
            android:name=".NotificationListener"
            android:label="SMS Notification Service"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true"
            android:foregroundServiceType="specialUse">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Receivers -->
        <receiver 
            android:name=".BootReceiver" 
            android:enabled="true" 
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
